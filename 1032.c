#include<stdio.h>
int main(){
    int n;
    char arr[50][50];
    scanf("%d", &n);
    for(int i = 0; i < n;i++)
        scanf("%s", &arr[i]);
    for(int i = 0; i<n;i++){
        for(int j = 0; j<n; j++){
        }
    }
}
