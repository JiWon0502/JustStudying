#include<stdio.h>

int chk(){ //다음 달리기 시작 가능 시간을 리턴
    
    
}

int main(){
    int N, M;
    int d[10001] = {0,};
    scanf("%d %d", &N, &M);
    for(int i = 0; i<M; i++)
        scanf("%d", d[i]);
}
